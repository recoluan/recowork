# 示例：手机端任务

用户想在手机里让 AI 协助写一份活动方案。

1. 用 `chat-mobile` target 生成启动指令和任务执行协议。
2. 先填写 `工作空间/任务简报.md`，明确活动对象、预算、时间和交付形式。
3. 把未确认的活动规模或渠道记录在 `待确认问题.md`，让 AI 先提问。
4. 将方案草稿放到 `02-任务产出/`，并让 AI 按检查清单自审。
5. 结束时生成续聊记忆卡，并在 `04-复盘与沉淀/` 记录下次可复用的方案结构。
